# Server Configuration
server.port=9000
server.max.clients=50
server.thread.pool.size=20

# Database Configuration (overridden by env vars in Docker)
db.host=localhost
db.port=5432
db.name=device_management
db.user=devmgmt
db.password=devmgmt@123
db.pool.size=10

# Security
security.token.expiry.hours=8
security.max.failed.login=5
security.lockout.minutes=15

# Logging
log.dir=logs
log.level=INFO
